type User {
    _id: ID
    mobile: String!
    password: String!
}

type Error {
    message: String!
    code: String!
}

type Query {
    getUser(_id: ID!): User
}

type Mutation {
    createUser(mobile: String!, password: String!): User
}

schema {
    query: Query
    mutation: Mutation
}
