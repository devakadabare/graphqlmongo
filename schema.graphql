type User {
    id: ID!
    mobile: String!
    password: String!
}

type Query {
    getUser(id: ID!): User
}

type Mutation {
    createUser(mobile: String!, password: String!): User
}

schema {
    query: Query
    mutation: Mutation
}
